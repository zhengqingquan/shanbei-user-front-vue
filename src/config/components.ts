// 引入组件
import {Button, Icon, NavBar, Tabbar, TabbarItem, Form, Field, CellGroup, List} from "vant";

const components = [Button, Icon, NavBar, Tabbar, TabbarItem, Form, Field, CellGroup, List];

export default components;