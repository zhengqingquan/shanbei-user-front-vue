// 引入组件
import {Button, Icon, NavBar, Tabbar, TabbarItem, Form, Field, CellGroup} from "vant";

const components = [Button, Icon, NavBar, Tabbar, TabbarItem, Form, Field, CellGroup];

export default components;